packages:
  # dbt Hub packages

  # codegen, macros to generate dbt code
  # https://hub.getdbt.com/dbt-labs/codegen/latest/
  - package: dbt-labs/codegen
    version: [">=0.7.0"]

  # dbt_utils, macros that can be (re)used across dbt projects
  # https://hub.getdbt.com/dbt-labs/dbt_utils/latest/
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0"]

  # dbt_project_evaluator, package highlights areas of a dbt project that are misaligned with dbt Labs' best practices
  # https://hub.getdbt.com/dbt-labs/dbt_project_evaluator/latest/
  - package: dbt-labs/dbt_project_evaluator
    version: [">=0.6.0"]

  # dbt_constraints, package to generate database constraints based on tests in a dbt project
  # https://hub.getdbt.com/snowflake-labs/dbt_constraints/latest/
  - package: Snowflake-Labs/dbt_constraints
    version: [">=0.6.0"]
